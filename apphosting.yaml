# Firebase App Hosting Configuration
# 这个文件配置了Firebase App Hosting的构建和运行时设置

# 运行时配置
runConfig:
  cpu: 1
  memoryMiB: 512
  maxInstances: 10
  minInstances: 0
  concurrency: 100

# 环境变量
env:
  - variable: NODE_ENV
    value: production
  - variable: HUGO_ENV
    value: production
